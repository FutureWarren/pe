# Samples

Place local demo inputs here.

Suggested phase 1 sample set:

- `data_room/` with PDF, XLSX, and CSV files from a sample data room
- `ai_workbook.xlsx`
- `gold_standard.xlsx`
- `template.xlsx` if used

This scaffold includes `samples/data_room/` as the default CLI input directory.
